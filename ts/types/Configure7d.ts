export type Configure7dInputs = {
  templateId: string;
  tokenAAddress: string;
  tokenBAddress: string;
  tokenALowerBound: string;
  tokenAUpperBound: string;
  loopCount: number;
  wAccount: string;
};
